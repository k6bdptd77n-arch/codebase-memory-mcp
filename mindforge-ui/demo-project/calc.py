"""A tiny calculator — the MindForge demo project. Deliberately small: the point is to watch
the plan -> parallel worktree agents -> verify gate -> merge loop run for real, on two
independent stories, not to admire the code itself."""


def add(a, b):
    return a + b


def subtract(a, b):
    return a - b
